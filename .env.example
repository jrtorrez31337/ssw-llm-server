# SSW LLM Server — Environment Configuration

# === Model Paths (mounted from host) ===
MODEL_REPO=/data/models
HEAVY_MODEL=Qwen/Qwen2.5-14B-Instruct-AWQ
LIGHT_MODEL=Qwen/Qwen3-8B-AWQ

# === vLLM Settings ===
HEAVY_GPU_MEMORY_UTILIZATION=0.50
LIGHT_GPU_MEMORY_UTILIZATION=0.40
VLLM_MAX_MODEL_LEN=8192
VLLM_TOOL_CALL_PARSER=hermes

# === Worker Ports (localhost-only, not exposed externally) ===
HEAVY_0_PORT=8001
LIGHT_0_PORT=8002
HEAVY_1_PORT=8003
LIGHT_1_PORT=8004

# === Redis ===
REDIS_HOST=redis
REDIS_PORT=6379

# === Gateway (public-facing) ===
GATEWAY_PORT=8000
